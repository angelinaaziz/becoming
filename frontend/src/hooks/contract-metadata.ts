// This file contains the contract metadata (ABI) for interacting with the Becoming contract
// It's imported from the compiled contract's metadata.json

import metadataJson from './contract-metadata.json';

// Export the metadata for use in the useContract hook
export const metadata = metadataJson; 